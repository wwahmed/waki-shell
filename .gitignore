node_modules/
*.log
.DS_Store
.env
.env.local
*.tsbuildinfo
# dist/ is intentionally tracked: dist/shell.json is the published
# contract that consuming apps fetch via raw.githubusercontent.com.

.claude/
